# RecruiterAI runtime dependencies
# --------------------------------
# The ranking step (rank.py) runs on the Python standard library ALONE -
# no third-party package, no network, no GPU - so it reproduces cleanly inside
# the Stage-3 sandbox (5 min, 16 GB RAM, CPU only).
#
# The packages below are OPTIONAL conveniences only:
#   - PyYAML   : nicer config parsing (a built-in mini-parser is used if absent)
#   - pytest   : to run the tests in tests/
#
# If you add the optional TF-IDF semantic re-rank (see README "Extending"),
# uncomment scikit-learn. It is NOT required for the default ranker.

PyYAML    # optional
pytest        # optional, tests only
# scikit-learn>=1.3  # optional, only if you enable the TF-IDF semantic re-rank
